- if @auth.nil?
  %fieldset
    %legend Welcome
    = link_to('Register', new_subscriber_path, :remote => true, :class => 'button radius')
    = link_to('Login', login_path, :remote => true, :id => 'login_btn', :class => 'button radius')