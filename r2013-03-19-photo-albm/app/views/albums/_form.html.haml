=form_for @album, :remote => true do |f|
  = f.label :name
  = f.text_field :name, :autofocus => true
  = f.submit 'Create Album', :class => 'button radius success'
  = link_to('Cancel', '#', :'data-clear-form' => true, :class => 'button radius alert', :id => 'cancel_btn')

